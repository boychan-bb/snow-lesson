<div class="container">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <h3>レッスン申し込みしたお客様一覧</h3>
            <% @bookings.each do |booking| %>
                <p><%= booking.user.name %></p>
            <% end %>
        </div>
    </div>
</div>